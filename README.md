# dsh-plugins

DeepSeek Harness (dsh) 插件集合仓库。

## 插件列表

| 插件 | 说明 |
|------|------|
| [dsh-ponytail](dsh-ponytail) | Lazy senior dev 模式插件 |

## 添加新插件

新建目录 `<plugin-name>/`，复制插件源码与 `package.json` 即可。
